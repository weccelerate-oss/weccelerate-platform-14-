# דוח QA - פורטל יזמים + אינטגרציית Pipedrive

**תאריך:** 2026-03-26
**סביבה:** Vercel Production + Next.js 16.1.1
**סוג בדיקות:** פונקציונלי, GUI, CRUD, הרשאות, E2E, חווית משתמש ושימושיות

---

## 🔴 קריטי (Critical)

### BUG-P01: אין יכולת העלאת קבצים מהפורטל
- **קטגוריה:** CRUD / E2E
- **תוצאה צפויה:** כפתור "העלאת קובץ" בכספת המסמכים מאפשר ליזם להעלות מסמכים (PDF, DOCX, XLSX, PPTX)
- **תוצאה בפועל:** כפתור "העלאת קובץ" לא מבצע שום פעולה — אין onClick, אין dialog, אין שום פונקציונליות. ה-API `/api/upload` תומך רק בתמונות (JPG, PNG, WebP) ולא במסמכים. ה-API routes של `/api/files/upload` ו-`/api/files/signed-url` מנוטרלים (disabled). כלומר — **אין שום דרך ליזם להעלות קבצים לפרויקט שלו**
- **חומרה:** 🔴 קריטי — פיצ'ר ליבה חסר לחלוטין

### BUG-P02: Pipedrive webhook לא מוגדר בפרודקשן
- **קטגוריה:** E2E / אינטגרציה
- **תוצאה צפויה:** כאשר deal ב-Pipedrive עובר שלב, ה-webhook מעדכן את הפרויקט בפורטל אוטומטית
- **תוצאה בפועל:** ה-webhook handler מחזיר 503 כי `PIPEDRIVE_WEBHOOK_SECRET` לא מוגדר ב-Vercel. קוד האינטגרציה מלא ופונקציונלי אבל חסרי ההגדרות
- **חומרה:** 🔴 קריטי — אין חיבור בין Pipedrive לפורטל

### BUG-P03: הפורטל מציג מסך onboarding כש-DB נכשל — ללא אינדיקציה
- **קטגוריה:** E2E / UX
- **תוצאה צפויה:** אם ה-DB לא זמין, להציג הודעת שגיאה
- **תוצאה בפועל:** `getProjectData()` עושה catch שקט ומחזיר `{ project: null }`, שמציג את מסך ה-Onboarding. היזם חושב שאין לו פרויקט, בעוד שזה באג DB
- **חומרה:** 🔴 קריטי

---

## 🟠 גבוה (High)

### BUG-P04: ניווט בסיידבר לא מחליף תוכן — כל הלינקים מובילים לאותו דף
- **קטגוריה:** GUI / ניווט / UX
- **תוצאה צפויה:** לחיצה על "הפרויקט שלי", "מסמכים", "התקדמות" מציגה תוכן רלוונטי
- **תוצאה בפועל:** כל ה-NAV_ITEMS מפנים ל-`/portal/dashboard` ורק מחליפים activeNav state — אבל אף לוגיקה לא משתמשת ב-activeNav כדי לסנן/להציג תוכן שונה
- **חומרה:** 🟠 גבוה

### BUG-P05: כפתור "פעולה חדשה" בהדר לא פונקציונלי
- **תוצאה צפויה:** פותח תפריט עם אפשרויות
- **תוצאה בפועל:** אין onClick handler
- **חומרה:** 🟠 גבוה

### BUG-P06: כפתור "סמן הכל כנקרא" בהתראות לא פונקציונלי
- **תוצאה צפויה:** מסמן את כל ההתראות כנקראו
- **תוצאה בפועל:** onClick מכיל TODO comment בלבד
- **חומרה:** 🟠 גבוה

### BUG-P07: "צפייה בכל ההתראות" — span סטטי ולא קישור
- **תוצאה צפויה:** קישור לדף התראות
- **תוצאה בפועל:** `<span>` ללא href או onClick
- **חומרה:** 🟠 גבוה

### BUG-P08: "צפייה בכל הפעילות" מוביל ל-404
- **תוצאה צפויה:** דף פעילות מלא
- **תוצאה בפועל:** מפנה ל-`/portal/activity` שלא קיים
- **חומרה:** 🟠 גבוה

### BUG-P09: חיפוש גלובלי בהדר לא מחפש כלום
- **תוצאה צפויה:** חיפוש עובד בתוך הפורטל
- **תוצאה בפועל:** שדה input נפתח אבל אין לוגיקת חיפוש
- **חומרה:** 🟠 גבוה

### BUG-P10: כפתור תצוגה מקדימה (Eye) בקבצים לא פונקציונלי
- **תוצאה צפויה:** פותח תצוגה מקדימה
- **תוצאה בפועל:** אין onClick handler
- **חומרה:** 🟠 גבוה

### BUG-P11: הורדת קבצי מוק נכשלת — URLs לא תקינים
- **תוצאה צפויה:** הורדת קבצים מהכספת עובדת
- **תוצאה בפועל:** URLs של מוק כמו `/files/business-plan.pdf` לא קיימים
- **חומרה:** 🟠 גבוה

### BUG-P12: משאבי Onboarding לא פונקציונליים
- **תוצאה צפויה:** כפתורי "מדריך ליזם" ו"תבנית תכנית עסקית" מובילים לתוכן
- **תוצאה בפועל:** כפתורים ללא href או onClick
- **חומרה:** 🟠 גבוה

### BUG-P13: WhatsApp button שולח ליזם עצמו במקום לצוות
- **קטגוריה:** לוגיקה
- **תוצאה צפויה:** כפתור WhatsApp שולח הודעה לצוות WeCcelerate
- **תוצאה בפועל:** `<WhatsAppButton phone={project.user.phone} />` — שולח ליזם עצמו
- **חומרה:** 🟠 גבוה

---

## 🟡 בינוני (Medium)

### BUG-P14: אין הגנת role בפורטל — כל משתמש מחובר יכול לגשת
- **קטגוריה:** הרשאות
- **תוצאה צפויה:** רק ENTREPRENEUR יכול לגשת לפורטל
- **תוצאה בפועל:** layout.tsx בודק רק session — לא בודק role
- **חומרה:** 🟡 בינוני

### BUG-P15: Timeline לא מסונכרן עם stage number
- **תוצאה צפויה:** timeline ו-stage תמיד מציגים אותו מצב
- **תוצאה בפועל:** timeline משתמש ב-status, progress bar ב-stage — עלולים להתנגש
- **חומרה:** 🟡 בינוני

### BUG-P16: Notification links מפנים לדפים שלא קיימים
- **תוצאה צפויה:** לחיצה על התראה מובילה לתוכן רלוונטי
- **תוצאה בפועל:** `/portal/calendar`, `/portal/documents`, `/portal/projects/X` — כולם 404
- **חומרה:** 🟡 בינוני

### BUG-P17: מסנני קבצים חסרים — אין סינון ל-IMAGE ו-VIDEO
- **תוצאה צפויה:** כל סוגי הקבצים ניתנים לסינון
- **תוצאה בפועל:** פילטר מציג רק ALL, DOCUMENT, SPREADSHEET, PRESENTATION
- **חומרה:** 🟡 בינוני

### BUG-P18: Footer ב-Onboarding — קישור "/contact" לא תקין
- **תוצאה צפויה:** קישור מוביל לדף צור קשר
- **תוצאה בפועל:** `/contact` לא קיים (דף הצור קשר ב-`/sites/main/contact`)
- **חומרה:** 🟡 בינוני

### BUG-P19: Onboarding progress bar תמיד 25%
- **תוצאה צפויה:** Progress bar מתעדכן
- **תוצאה בפועל:** `completedSteps` הוא state קבוע — לא מתעדכן
- **חומרה:** 🟡 בינוני

### BUG-P20: "הודעות" במובייל bottom nav חסר סימון "בקרוב"
- **תוצאה צפויה:** "הודעות" מסומן כ-comingSoon כמו בסיידבר
- **תוצאה בפועל:** MOBILE_BOTTOM_NAV אין בו comingSoon
- **חומרה:** 🟡 בינוני

---

## 🟢 נמוך (Low)

### BUG-P21: typeof window check מיותר ב-Quick Actions
- **תוצאה בפועל:** `typeof window === 'undefined' ? '972555647538' : '972555647538'` — שני הצדדים זהים
- **חומרה:** 🟢 נמוך

### BUG-P22: אין spinner במהלך הורדת קובץ
- **תוצאה צפויה:** loading indication במהלך הורדה
- **תוצאה בפועל:** ישר CheckCircle אחרי ההורדה, אין spinner בזמן
- **חומרה:** 🟢 נמוך

---

## סיכום

| חומרה | כמות |
|--------|-------|
| 🔴 קריטי | 3 |
| 🟠 גבוה | 10 |
| 🟡 בינוני | 7 |
| 🟢 נמוך | 2 |
| **סה"כ** | **22** |
