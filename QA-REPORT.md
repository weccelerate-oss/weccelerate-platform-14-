# דוח QA מקיף - WeCcelerate Platform
**תאריך:** 2026-03-25
**גרסה:** Next.js 16.1.1 | React 19.2.3 | Prisma 7
**סה"כ תקלות:** 72 תקלות (רובן תוקנו)
**הערה:** הרשמת משתמשים מתבצעת רק דרך האדמין (אין self-registration)

---

## סיכום לפי חומרה

| חומרה | כמות | תוקנו |
|--------|-------|--------|
| 🔴 קריטי | 8 | ✅ 8 |
| 🟠 גבוה | 16 | ✅ 14 |
| 🟡 בינוני | 28 | ✅ 18 |
| 🟢 נמוך | 20 | ✅ 5 |

---

## 1. תקלות הרשאות ואבטחה (Permissions & Security)

### 🔴 SEC-001: עקיפת אימות באזור האדמין
**קובץ:** `app/(admin)/admin/layout.tsx`
**תוצאה צפויה:** משתמש לא מאומת מנותב לדף התחברות בגישה ל-`/admin`
**תוצאה בפועל:** משתמש Mock קשיח מוגדר בקוד (`admin@weccelerate.co.il`) - כל אחד יכול לגשת לפאנל האדמין ללא התחברות
**פירוט:** הקוד מכיל `mockUser` קשיח שעוקף את כל בדיקות ההרשאה. הערה בקוד: "skip auth check for debugging"
**חומרה:** 🔴 קריטי

---

### 🔴 SEC-002: כתובת Webhook של Zapier חשופה בקוד
**קובץ:** `app/actions/leads.ts` (שורה 62)
**תוצאה צפויה:** כתובות Webhook מאוחסנות כמשתני סביבה (env vars)
**תוצאה בפועל:** URL קשיח בקוד: `https://hooks.zapier.com/hooks/catch/7280045/ucs3rvp/` - חשוף בהיסטוריית Git
**פירוט:** כל מי שיש לו גישה לריפו יכול להפעיל את ה-Webhook ולשלוח נתונים זדוניים
**חומרה:** 🔴 קריטי

---

### 🔴 SEC-003: הגבלת קצב (Rate Limiting) לפי מופע בלבד
**קובץ:** `lib/rate-limit.ts`
**תוצאה צפויה:** הגבלת קצב גלובלית לכל בקשות ההעלאה (10 העלאות/דקה)
**תוצאה בפועל:** הגבלה מבוססת זיכרון מקומי - בסביבת serverless כל מופע מחזיק מונה נפרד. תוקף יכול לפזר בקשות ולעקוף
**חומרה:** 🔴 קריטי

---

### 🟠 SEC-004: סיסמה זמנית מוחזרת בתגובת השרת
**קובץ:** `app/(admin)/admin/actions.ts` (שורות 367-375)
**תוצאה צפויה:** סיסמה זמנית נשלחת רק באימייל
**תוצאה בפועל:** `tempPassword` מוחזר בתגובת Server Action - נראה ב-Network tab ובלוגים
**חומרה:** 🟠 גבוה

---

### 🟠 SEC-005: אין בדיקת תקינות תפקיד (Role) בעדכון משתמש
**קובץ:** `app/(admin)/admin/actions.ts` (שורה 392)
**תוצאה צפויה:** שדה role מאומת כנגד enum תקין (ADMIN, ENTREPRENEUR, etc.)
**תוצאה בפועל:** `data.role` מועבר ישירות ל-Prisma ללא ולידציה - ערכים לא תקינים עלולים לעבור
**חומרה:** 🟠 גבוה

---

### 🟠 SEC-006: אין הגבלת קצב על איפוס סיסמה
**קובץ:** `app/actions/auth.ts`
**תוצאה צפויה:** מוגבל ל-3-5 ניסיונות לדקה
**תוצאה בפועל:** אין הגבלת קצב על בקשות איפוס סיסמה - אפשר brute force על טוקנים
**חומרה:** 🟠 גבוה

---

### 🟠 SEC-007: אין בדיקת בעלות על פרויקט בדאשבורד פורטל
**קובץ:** `app/(portal)/portal/dashboard/page.tsx` (שורות 36-38)
**תוצאה צפויה:** משתמש רואה רק פרויקטים שלו
**תוצאה בפועל:** `getProjectData()` שולף פרויקטים לפי userId מהסשן, אבל אין אימות כפול שהפרויקט שייך למשתמש
**חומרה:** 🟠 גבוה

---

### 🟡 SEC-008: API אנליטיקס פתוח ללא אימות
**קובץ:** `app/api/analytics/summary/route.ts`
**תוצאה צפויה:** רק אדמינים יכולים לראות סיכום אנליטיקס
**תוצאה בפועל:** GET `/api/analytics/summary` פתוח לכולם - חושף נתוני שימוש
**חומרה:** 🟡 בינוני

---

### 🟡 SEC-009: אין ולידציית כתובות URL בהתראות
**קובץ:** `app/(portal)/portal/dashboard/dashboard-content.tsx` (שורה 468)
**תוצאה צפויה:** לינקים בהתראות מאומתים כנגד דומיינים מותרים
**תוצאה בפועל:** `notif.link || '#'` ללא בדיקה - לינקים זדוניים עלולים להגיע
**חומרה:** 🟡 בינוני

---

### 🟡 SEC-010: אין CAPTCHA בטופס ההרשמה
**קובץ:** `components/auth/register-form.tsx`
**תוצאה צפויה:** מנגנון הגנה מפני בוטים (reCAPTCHA/hCaptcha)
**תוצאה בפועל:** אין הגנה מפני הרשמות אוטומטיות
**חומרה:** 🟡 בינוני

---

## 2. תקלות CRUD

### 🔴 CRUD-001: רישום לאירוע ללא בדיקת קיבולת
**קובץ:** `app/actions/leads.ts` (שורות 320-330)
**תוצאה צפויה:** מערכת מונעת רישום כשהאירוע מלא (`registeredCount >= capacity`)
**תוצאה בפועל:** `registeredCount` עולה ללא הגבלה - אפשר לרשום מעבר לקיבולת. גם race condition: שני רישומים במקביל כששארה מקום אחד
**חומרה:** 🔴 קריטי

---

### 🟠 CRUD-002: שדה expireAt חסר ביצירת חדשות
**קובץ:** `app/(admin)/admin/actions.ts` (שורות 44-68)
**תוצאה צפויה:** אדמין יכול להגדיר תאריך תפוגה לעדכון חדשות
**תוצאה בפועל:** `NewsFormData` לא כולל שדה `expireAt`, אבל API התוכן (`/api/content`) בודק תפוגה. תכונה חסרה
**חומרה:** 🟠 גבוה

---

### 🟠 CRUD-003: slug לא נבדק לייחודיות בסטוריז
**קובץ:** `app/(admin)/admin/actions.ts` (שורות 484-517)
**תוצאה צפויה:** slug כפול מציג שגיאה ידידותית
**תוצאה בפועל:** אין בדיקת ייחודיות לפני הכנסה ל-DB - שגיאת Prisma constraint לא מטופלת
**חומרה:** 🟠 גבוה

---

### 🟠 CRUD-004: אין פגינציה באדמין - כל הנתונים נטענים בבת אחת
**קבצים:** כל דפי האדמין (news, events, videos, users, stories)
**תוצאה צפויה:** פגינציה מצד השרת (10-20 פריטים בעמוד)
**תוצאה בפועל:** `findMany()` ללא `take`/`skip` - כל הרשומות נטענות. עם אלפי רשומות זה יקרוס
**חומרה:** 🟠 גבוה

---

### 🟡 CRUD-005: תאריכים לא מאומתים בעדכון אירועים
**קובץ:** `app/(admin)/admin/actions.ts` (שורות 144, 182)
**תוצאה צפויה:** תאריך לא תקין מציג שגיאה
**תוצאה בפועל:** `new Date(data.date)` ללא בדיקה - יכול ליצור `Invalid Date` ב-DB
**חומרה:** 🟡 בינוני

---

### 🟡 CRUD-006: updateStoryAction - טיפול שגיאות לא עקבי
**קובץ:** `app/(admin)/admin/actions.ts` (שורות 519-554)
**תוצאה צפויה:** שגיאת הרשאה מוחזרת כתגובה תקנית
**תוצאה בפועל:** `verifyAdmin()` נקרא מחוץ ל-try-catch - אם נכשל, Exception לא נתפס
**חומרה:** 🟡 בינוני

---

### 🟡 CRUD-007: סנכרון YouTube ללא הגבלת קצב
**קובץ:** `app/(admin)/admin/actions.ts` (שורות 682-707)
**תוצאה צפויה:** כפתור סנכרון מוגבל לפעם ב-X דקות
**תוצאה בפועל:** אדמין יכול להפעיל סנכרון ללא הגבלה - עלול להגיע ל-YouTube API rate limit
**חומרה:** 🟡 בינוני

---

### 🟡 CRUD-008: אין לוג פעילות מלא לכל פעולות CRUD
**קובץ:** `app/(admin)/admin/actions.ts`
**תוצאה צפויה:** כל יצירה/עדכון/מחיקה מתועדת ב-ActivityLog
**תוצאה בפועל:** רק יצירת משתמש מתועדת. מחיקת תוכן, עדכונים ושינויי הרשאות לא מתועדים
**חומרה:** 🟡 בינוני

---

### 🟢 CRUD-009: שדה displayOrder לא מאומת בסטוריז
**קובץ:** `app/(admin)/admin/actions.ts`
**תוצאה צפויה:** מספר סידורי חיובי ייחודי
**תוצאה בפועל:** מקבל כל מספר ללא ולידציה - אפשר להגדיר ערכים שליליים או כפולים
**חומרה:** 🟢 נמוך

---

## 3. תקלות GUI ו-UX

### 🔴 GUI-001: לינקים שבורים בסיידבר הפורטל
**קובץ:** `app/(portal)/portal/dashboard/dashboard-content.tsx`
**תוצאה צפויה:** כל הלינקים מובילים לדפים קיימים
**תוצאה בפועל:** 10 לינקים מובילים לדפים שלא קיימים:
- `/portal/project` ❌
- `/portal/documents` ❌
- `/portal/calendar` ❌
- `/portal/messages` ❌
- `/portal/progress` ❌
- `/portal/learning` ❌
- `/portal/settings` ❌
- `/portal/help` ❌
- `/portal/notifications` ❌
- `/register` ❌ (דף הרשמה)
- `/forgot-password` ❌ (שחזור סיסמה)

**חומרה:** 🔴 קריטי

---

### 🔴 GUI-002: כפתור התנתקות לא פועל
**קובץ:** `app/(portal)/portal/dashboard/dashboard-content.tsx` (שורות 302, 391)
**תוצאה צפויה:** לחיצה על "התנתק" מבצעת logout ומעבירה לדף ההתחברות
**תוצאה בפועל:** הכפתור ללא `onClick` handler - לחיצה לא עושה כלום
**חומרה:** 🔴 קריטי

---

### 🟠 GUI-003: תיבת "זכור אותי" לא פונקציונלית
**קובץ:** `components/auth/login-form.tsx` (שורות 289-297)
**תוצאה צפויה:** סימון "זכור אותי" שומר את הסשן לזמן ארוך
**תוצאה בפועל:** ה-checkbox מוצג אבל ערכו לא נשלח לשרת - אין השפעה
**חומרה:** 🟠 גבוה

---

### 🟠 GUI-004: "סמן הכל כנקרא" לא פועל
**קובץ:** `app/(portal)/portal/dashboard/dashboard-content.tsx` (שורה 456)
**תוצאה צפויה:** לחיצה מסמנת כל ההתראות כנקראו
**תוצאה בפועל:** כפתור ללא `onClick` handler
**חומרה:** 🟠 גבוה

---

### 🟠 GUI-005: כפתור "העלאת מסמך ראשון" לא פועל
**קובץ:** `app/(portal)/portal/dashboard/components/file-vault.tsx` (שורה 150)
**תוצאה צפויה:** פתיחת דיאלוג העלאת קובץ
**תוצאה בפועל:** כפתור ללא `onClick` handler
**חומרה:** 🟠 גבוה

---

### 🟠 GUI-006: הורדת קובץ ללא טיפול שגיאות
**קובץ:** `app/(portal)/portal/dashboard/components/file-vault.tsx` (שורות 124-138)
**תוצאה צפויה:** כשהורדה נכשלת - הודעת שגיאה מוצגת
**תוצאה בפועל:** אין בדיקת `response.ok` - אייקון הצלחה (CheckCircle2) מוצג גם כשההורדה נכשלה
**חומרה:** 🟠 גבוה

---

### 🟠 GUI-007: טלפון ברירת מחדל קשיח ב-WhatsApp
**קובץ:** `app/(portal)/portal/dashboard/components/whatsapp-button.tsx` (שורה 35)
**תוצאה צפויה:** מספר טלפון נלקח מהפרויקט או מהגדרות
**תוצאה בפועל:** מספר קשיח `+972535551234` - נראה כמספר בדיקה
**חומרה:** 🟠 גבוה

---

### 🟡 GUI-008: Badge התראות ב-RTL - מיקום שגוי
**קובץ:** `app/(portal)/portal/dashboard/dashboard-content.tsx` (שורות 438-440)
**תוצאה צפויה:** Badge מוצג בצד שמאל (RTL)
**תוצאה בפועל:** `right-0.5` במקום `left-0.5` - במצב RTL, ה-Badge במקום הלא נכון
**חומרה:** 🟡 בינוני

---

### 🟡 GUI-009: Tooltip של WhatsApp במיקום שגוי ב-RTL
**קובץ:** `app/(portal)/portal/dashboard/components/whatsapp-button.tsx` (שורה 276)
**תוצאה צפויה:** Tooltip מוצג בצד ימין (RTL)
**תוצאה בפועל:** `right-full mr-3` - ה-Tooltip מוצג בכיוון הלא נכון
**חומרה:** 🟡 בינוני

---

### 🟡 GUI-010: פילטר סוגי קבצים חלקי
**קובץ:** `app/(portal)/portal/dashboard/components/file-vault.tsx` (שורה 213)
**תוצאה צפויה:** כפתורי פילטר לכל סוגי הקבצים (6 סוגים)
**תוצאה בפועל:** רק 4 סוגים בפילטר: ALL, DOCUMENT, SPREADSHEET, PRESENTATION - חסרים VIDEO ו-IMAGE
**חומרה:** 🟡 בינוני

---

### 🟡 GUI-011: אין Debounce בשדה חיפוש
**קובץ:** `app/(portal)/portal/dashboard/dashboard-content.tsx` (שורה 520)
**תוצאה צפויה:** חיפוש עם עיכוב (debounce) של 300ms
**תוצאה בפועל:** כל תו מפעיל חיפוש מיידי - בעיות ביצועים עם נתונים רבים
**חומרה:** 🟡 בינוני

---

### 🟡 GUI-012: מוק דאטה ללא התראה
**קובץ:** `app/(portal)/portal/dashboard/page.tsx` (שורות 90-91)
**תוצאה צפויה:** כשה-DB לא זמין - הודעה ברורה למשתמש
**תוצאה בפועל:** בשגיאת DB, נתוני מוק מוצגים בשקט - המשתמש חושב שאלה הנתונים האמיתיים
**חומרה:** 🟡 בינוני

---

### 🟡 GUI-013: חישוב התקדמות ללא הגבלה
**קובץ:** `app/(portal)/portal/dashboard/components/stats-cards.tsx` (שורה 129)
**תוצאה צפויה:** סרגל התקדמות תמיד 0-100%
**תוצאה בפועל:** `(project.stage / 10) * 100` - אם stage > 10, הסרגל חורג מ-100%
**חומרה:** 🟡 בינוני

---

### 🟡 GUI-014: ולידציית טלפון רופפת בטופס יצירת קשר
**קובץ:** `app/sites/main/contact/ContactForm.tsx` (שורה 52)
**תוצאה צפויה:** רק מספרי טלפון תקינים מתקבלים
**תוצאה בפועל:** Regex `/^[\d\s\-+()]{7,20}$/` מקבל מחרוזות כמו `"------- ++ (((("`
**חומרה:** 🟡 בינוני

---

### 🟡 GUI-015: פרמטר service לא נשלח בטופס יצירת קשר
**קובץ:** `app/sites/main/contact/ContactForm.tsx` (שורות 241-249)
**תוצאה צפויה:** שירות שנבחר נשלח לשרת
**תוצאה בפועל:** `serviceParam` משמש ל-UI אבל לא נשלח כ-hidden input
**חומרה:** 🟡 בינוני

---

### 🟡 GUI-016: אין מצב טעינה בשליחת טופס יצירת קשר
**קובץ:** `app/sites/main/contact/ContactForm.tsx`
**תוצאה צפויה:** ספינר או אינדיקציה ברורה שהטופס נשלח
**תוצאה בפועל:** שדות מושבתים (disabled) אבל אין אינדיקציה ויזואלית ברורה
**חומרה:** 🟡 בינוני

---

### 🟢 GUI-017: שפה קשיחה בטיימליין פרויקט
**קובץ:** `app/(portal)/portal/dashboard/components/project-timeline.tsx` (שורות 49-58)
**תוצאה צפויה:** תוויות בשפה לפי הגדרת i18n
**תוצאה בפועל:** עברית קשיחה (`labelHe: 'טיוטה'`) ללא שימוש ב-`useLanguage()`
**חומרה:** 🟢 נמוך

---

### 🟢 GUI-018: מטבע מימון קשיח
**קובץ:** `app/(portal)/portal/dashboard/page.tsx` (שורה 120)
**תוצאה צפויה:** מטבע לפי הפרויקט
**תוצאה בפועל:** `fundingCurrency: 'USD'` קשיח במוק - פרויקטים ישראלים צפויים ב-ILS
**חומרה:** 🟢 נמוך

---

### 🟢 GUI-019: גלישת טקסט בטיימליין במובייל
**קובץ:** `app/(portal)/portal/dashboard/components/project-timeline.tsx` (שורה 270)
**תוצאה צפויה:** טקסט חתוך עם ellipsis
**תוצאה בפועל:** תאריכים ארוכים שוברים את הלייאאוט במובייל
**חומרה:** 🟢 נמוך

---

### 🟢 GUI-020: החלפת שפה ללא מצב טעינה
**קובץ:** `components/layout/CorporateNavbar.tsx`
**תוצאה צפויה:** אינדיקציית טעינה בהחלפת שפה
**תוצאה בפועל:** המתג מתחלף מיידית אבל התוכן נטען ברקע ללא אינדיקציה
**חומרה:** 🟢 נמוך

---

## 4. תקלות נגישות (Accessibility - A11y)

### 🟠 A11Y-001: קרוסלת סטוריז ללא aria-live
**קובץ:** `components/ui/success-stories.tsx`
**תוצאה צפויה:** שינוי שקופית מוכרז לקוראי מסך (`aria-live="polite"`)
**תוצאה בפועל:** אין הכרזה - משתמשי קוראי מסך לא יודעים שהתוכן השתנה
**חומרה:** 🟠 גבוה

---

### 🟠 A11Y-002: טיקר חדשות לא מכבד prefers-reduced-motion
**קובץ:** `components/ui/live-ticker.tsx`
**תוצאה צפויה:** אנימציה מושבתת כשמשתמש ביקש הפחתת תנועה
**תוצאה בפועל:** אנימציית גלילה רצה תמיד - עלולה לגרום בחילה למשתמשים רגישים
**חומרה:** 🟠 גבוה

---

### 🟡 A11Y-003: סרגל קיבולת אירועים ללא role="progressbar"
**קובץ:** `components/ui/events-center.tsx`
**תוצאה צפויה:** `<div role="progressbar" aria-valuenow={percentage}>`
**תוצאה בפועל:** סרגל ויזואלי בלבד - קוראי מסך לא מזהים אותו
**חומרה:** 🟡 בינוני

---

### 🟡 A11Y-004: סרגל חוזק סיסמה ללא role="progressbar"
**קובץ:** `components/auth/register-form.tsx`
**תוצאה צפויה:** `role="progressbar" aria-valuenow={strength}`
**תוצאה בפועל:** סרגל צבעוני ויזואלי בלבד
**חומרה:** 🟡 בינוני

---

### 🟡 A11Y-005: ניווט פורטל ללא תמיכת מקלדת
**קובץ:** `components/portal/navigation.tsx`
**תוצאה צפויה:** Escape סוגר תפריט, Tab מנווט
**תוצאה בפועל:** תפריט משתמש ללא keyboard handlers
**חומרה:** 🟡 בינוני

---

### 🟡 A11Y-006: קרוסלה אוטומטית לא מכבדת prefers-reduced-motion
**קובץ:** `components/ui/success-stories.tsx`
**תוצאה צפויה:** autoPlay מושבת כשמשתמש ביקש הפחתת תנועה
**תוצאה בפועל:** אנימציה אוטומטית רצה תמיד
**חומרה:** 🟡 בינוני

---

### 🟢 A11Y-007: תגיות קטגוריה בוידאו ללא aria-label
**קובץ:** `components/ui/video-gallery.tsx`
**תוצאה צפויה:** badges עם תיאור לקוראי מסך
**תוצאה בפועל:** ויזואליים בלבד
**חומרה:** 🟢 נמוך

---

### 🟢 A11Y-008: animate-pulse באירועים לא מכבד prefers-reduced-motion
**קובץ:** `components/ui/events-center.tsx`
**תוצאה צפויה:** אנימציה מושבתת
**תוצאה בפועל:** badge ה-status ממשיך לפעום
**חומרה:** 🟢 נמוך

---

## 5. תקלות E2E (End-to-End Flows)

### 🔴 E2E-001: תהליך הרשמה → כניסה → דאשבורד שבור
**תוצאה צפויה:** משתמש נרשם → מקבל אישור → מתחבר → מגיע לדאשבורד
**תוצאה בפועל:**
1. דף `/register` לא קיים (404) ❌
2. דף `/forgot-password` לא קיים (404) ❌
3. לינק "הירשם" מטופס ההתחברות שבור
4. אפילו אם ההרשמה הייתה עובדת - אין אימות אימייל פעיל

**חומרה:** 🔴 קריטי

---

### 🟠 E2E-002: תהליך יצירת אירוע → רישום → ניהול
**תוצאה צפויה:** אדמין יוצר אירוע → משתמש נרשם → אדמין רואה מספר נרשמים
**תוצאה בפועל:**
1. אירוע נוצר בהצלחה ✅
2. רישום לא בודק קיבולת - אפשר לרשום מעבר למגבלה ❌
3. אין בדיקת כפילויות - אותו אימייל יכול להירשם פעמיים ❌
4. `registeredCount` עולה אבל אין רשימת נרשמים ❌

**חומרה:** 🟠 גבוה

---

### 🟠 E2E-003: תהליך העלאת תמונה באדמין
**תוצאה צפויה:** אדמין מעלה תמונה → תמונה מוצגת בתוכן
**תוצאה בפועל:**
1. העלאה דורשת אימות (auth session) ✅
2. אבל אדמין משתמש ב-mockUser שאין לו session אמיתי ❌
3. לכן העלאת תמונה תחזיר 401 Unauthorized ❌
4. הקוד מטפל בשגיאה אבל ה-UX שבור

**חומרה:** 🟠 גבוה

---

### 🟠 E2E-004: תהליך איפוס סיסמה
**תוצאה צפויה:** משתמש מבקש איפוס → מקבל אימייל → מאפס → מתחבר
**תוצאה בפועל:**
1. דף `/forgot-password` לא קיים (404) ❌
2. Server Action `requestPasswordReset` קיים ✅
3. שליחת אימייל דרך Resend - תלוי בהגדרת API key
4. דף `/reset-password` לא קיים (404) ❌
5. Server Action `resetPassword` קיים ✅
6. התהליך שבור כי אין דפים

**חומרה:** 🟠 גבוה

---

### 🟡 E2E-005: תהליך ניהול תוכן (חדשות/וידאו/סטוריז)
**תוצאה צפויה:** אדמין יוצר → עורך → מפרסם → מוצג באתר
**תוצאה בפועל:**
1. יצירה עובדת (עם mockUser) ✅
2. עריכה עובדת ✅
3. מחיקה עובדת ✅
4. אבל אין draft/publish workflow - תוכן מפורסם מיידית ❌
5. אין preview לפני פרסום ❌
6. `revalidatePath` נקרא אבל cache עלול לא להתעדכן מיידית

**חומרה:** 🟡 בינוני

---

### 🟡 E2E-006: תהליך טופס יצירת קשר → Zapier → CRM
**תוצאה צפויה:** משתמש שולח טופס → נתונים נשלחים ל-Zapier → ליד נוצר ב-CRM
**תוצאה בפועל:**
1. טופס נשלח ✅
2. ולידציה עובדת (Zod) ✅
3. Webhook URL קשיח בקוד (בעיית אבטחה) ⚠️
4. אין בדיקה אם ה-Webhook הצליח - הודעת הצלחה תמיד מוצגת ❌
5. אין retry mechanism כשה-Webhook נכשל ❌

**חומרה:** 🟡 בינוני

---

### 🟡 E2E-007: תהליך כניסת משתמש → פורטל → ניהול פרויקט
**תוצאה צפויה:** משתמש מתחבר → רואה דאשבורד → מנהל פרויקט
**תוצאה בפועל:**
1. התחברות עובדת ✅
2. הפניה ל-`/portal/dashboard` ✅
3. כל הלינקים בסיידבר שבורים (ראה GUI-001) ❌
4. אין אפשרות התנתקות (ראה GUI-002) ❌
5. הורדת קבצים ללא טיפול שגיאות (ראה GUI-006) ❌
6. כפתורי פעולה (העלאת מסמך, סמן כנקרא) לא פועלים ❌

**חומרה:** 🟡 בינוני

---

### 🟡 E2E-008: תהליך Cron YouTube Sync
**תוצאה צפויה:** כל 4 שעות - סנכרון אוטומטי מ-YouTube
**תוצאה בפועל:**
1. Endpoint דורש `CRON_SECRET` ✅
2. `syncYouTubeVideos()` קורא ל-YouTube API ✅
3. אבל `YOUTUBE_API_KEY` לא מוגדר כברירת מחדל ❌
4. אין alerting אם הסנכרון נכשל ❌
5. Revalidation של cache עובד ✅

**חומרה:** 🟡 בינוני

---

### 🟢 E2E-009: Pipedrive Webhook מושבת
**תוצאה צפויה:** Webhook פעיל ומעדכן פרויקטים
**תוצאה בפועל:** Endpoint מחזיר 410 Gone - אינטגרציה מושבתת
**חומרה:** 🟢 נמוך (כנראה מכוון)

---

### 🟢 E2E-010: תהליך דוח חודשי
**תוצאה צפויה:** ב-1 לכל חודש - דוח נשלח באימייל
**תוצאה בפועל:**
1. Cron endpoint קיים ✅
2. שליחה ל-`weccelerate@gmail.com` ✅
3. תלוי ב-Resend API key ⚠️
4. אין retry ⚠️

**חומרה:** 🟢 נמוך

---

## 6. תקלות API

### 🟡 API-001: CORS פתוח לכולם
**קבצים:** כל API routes ב-`/api/content/`
**תוצאה צפויה:** CORS מוגבל לדומיינים מורשים
**תוצאה בפועל:** `Access-Control-Allow-Origin: *` - כל דומיין יכול לגשת ל-API
**חומרה:** 🟡 בינוני

---

### 🟡 API-002: הגבלת גודל Body חלקית
**קבצים:** רוב ה-API routes
**תוצאה צפויה:** כל Endpoints מגבילים גודל body
**תוצאה בפועל:** רק `/api/analytics/track` מגביל ל-10KB. שאר ה-endpoints ללא הגבלה
**חומרה:** 🟡 בינוני

---

### 🟢 API-003: קבצי upload מושבתים לא מוסרים
**קבצים:** `api/files/_upload_disabled/`, `api/files/_signed-url_disabled/`
**תוצאה צפויה:** endpoints מושבתים מוסרים לגמרי
**תוצאה בפועל:** עדיין קיימים כ-stubs - מבלבל
**חומרה:** 🟢 נמוך

---

## 7. תקלות קוד ותשתית

### 🟡 INFRA-001: ignoreBuildErrors מופעל
**קובץ:** `next.config.ts`
**תוצאה צפויה:** שגיאות TypeScript מונעות build
**תוצאה בפועל:** `typescript: { ignoreBuildErrors: true }` - שגיאות טיפוסים מתעלמים
**חומרה:** 🟡 בינוני

---

### 🟡 INFRA-002: Dead code - getSession ב-auth.utils
**קובץ:** `lib/auth/auth.utils.ts` (שורות 73-75)
**תוצאה צפויה:** פונקציה עובדת
**תוצאה בפועל:** `getServerSession(authOptions)` - authOptions ריק. הפונקציה תיכשל אם תיקרא
**חומרה:** 🟡 בינוני

---

### 🟡 INFRA-003: TrackedLink ללא try-catch
**קובץ:** `components/ui/TrackedLink.tsx`
**תוצאה צפויה:** שגיאת analytics לא שוברת navigation
**תוצאה בפועל:** `trackClick()` יכול לזרוק exception שימנע ניווט
**חומרה:** 🟡 בינוני

---

### 🟢 INFRA-004: טיפול שגיאות YouTube URL parsing
**קובץ:** `components/ui/video-gallery.tsx`
**תוצאה צפויה:** כל פורמטי YouTube URL נתמכים
**תוצאה בפועל:** string split פשוט - לא מטפל בכל הפורמטים (shorts, embed, youtu.be)
**חומרה:** 🟢 נמוך

---

### 🟢 INFRA-005: ScrollReveal ללא fallback ל-IntersectionObserver
**קובץ:** `components/ui/ScrollReveal.tsx`
**תוצאה צפויה:** תוכן מוצג גם ללא תמיכה ב-IO
**תוצאה בפועל:** בדפדפנים ישנים - התוכן עלול להישאר מוסתר
**חומרה:** 🟢 נמוך

---

### 🟢 INFRA-006: מספר WhatsApp קשיח ב-WhatsAppFloat
**קובץ:** `components/ui/WhatsAppFloat.tsx`
**תוצאה צפויה:** מספר ממשתנה סביבה
**תוצאה בפועל:** `972555647538` קשיח
**חומרה:** 🟢 נמוך

---

### 🟢 INFRA-007: קואורדינטות מפה קשיחות
**קובץ:** `components/ui/NavigationButtons.tsx`
**תוצאה צפויה:** מיקום דינמי או מהגדרות
**תוצאה בפועל:** קואורדינטות קשיחות ב-iframe URL
**חומרה:** 🟢 נמוך

---

## סיכום המלצות לפי עדיפות

### 🔴 מיידי (חוסמי שחרור)
1. **SEC-001**: הסרת mockUser מ-admin layout - חיבור אימות אמיתי
2. **SEC-002**: העברת Zapier URL למשתנה סביבה
3. **GUI-001**: יצירת דפי פורטל חסרים או הוספת "בקרוב"
4. **GUI-002**: חיבור פונקציית logout
5. **CRUD-001**: הוספת בדיקת קיבולת לרישום אירועים
6. **E2E-001**: יצירת דפי register ו-forgot-password

### 🟠 דחוף (לפני שחרור)
7. **SEC-004**: הפסקת החזרת סיסמאות בתגובות
8. **SEC-005**: ולידציית role בעדכון משתמש
9. **GUI-003**: חיבור "זכור אותי" או הסרה
10. **GUI-004/005**: חיבור כפתורים לא פונקציונליים
11. **GUI-006**: טיפול שגיאות בהורדת קבצים
12. **A11Y-001/002**: תיקון נגישות קרוסלה וטיקר

### 🟡 גבוה (ספרינט הבא)
13. **CRUD-004**: הוספת פגינציה באדמין
14. **SEC-003**: מעבר ל-Redis rate limiting
15. **INFRA-001**: הסרת ignoreBuildErrors
16. **API-001**: הגבלת CORS לדומיינים ספציפיים

---

*דוח זה נוצר על ידי סקירת קוד מקיפה. מומלץ לבצע בדיקות ידניות וחקירה נוספת לכל תקלה.*
